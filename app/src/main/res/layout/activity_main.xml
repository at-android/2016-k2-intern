<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:custom="http://schemas.android.com/apk/res/vn.asiantech.training.views"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <vn.asiantech.training.views.HeaderOptionsView
        android:id="@+id/header_login"
        android:layout_width="match_parent"
        android:layout_height="?android:attr/listPreferredItemHeight"
        app:isShowBtnBack="false"
        app:isShowBtnNext="false"
        app:isShowTvHome="false"
        app:isShowTvTitle="true"
        app:title="@string/header_tv_login"
        />
    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/header_padding"
        android:hint="@string/main_edt_username"
        android:id="@+id/edtUsername"
        android:layout_below="@id/header_login"
        android:inputType="text"
        />
    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/header_padding"
        android:hint="@string/main_edt_password"
        android:id="@+id/edtPassword"
        android:inputType="textPassword"
        android:layout_below="@id/edtUsername"
        android:layout_marginTop="@dimen/header_padding"
        />
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:text="@string/main_btn_login"
        android:id="@+id/btnLogin"
        android:layout_below="@id/edtPassword"
        android:layout_marginTop="@dimen/header_padding"
        />
</RelativeLayout>
